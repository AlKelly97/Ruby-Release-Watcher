<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Release Watcher</title>
    <style>
      body { font-family: system-ui, sans-serif; margin: 2rem; }
      table { border-collapse: collapse; width: 100%; max-width: 800px; }
      th, td { border: 1px solid #ccc; padding: 0.5rem; text-align: left; }
      th { background: #f5f5f5; }
    </style>
  </head>
  <body>
    <h1>Release Watcher</h1>

    <!-- Flash Message Area -->
    <% if flash[:success] %>
      <div style="background: #d4edda; color: #155724; padding: 1rem; margin-bottom: 1rem; border-radius: 4px;">
        <%= flash[:success] %>
      </div>
    <% end %>

    <!-- Add Project form -->
    <form action="/projects" method="post" style="margin-bottom: 2rem;">
      <input type="text" name="name" placeholder="Project Name" required style="padding: 0.5rem; margin-right: 0.5rem;">
      <input type="text" name="source" placeholder="Source (e.g., Nintendo)" required style="padding: 0.5rem; margin-right: 0.5rem;">
      <input type="url" name="url" placeholder="Project URL" required style="padding: 0.5rem; margin-right: 0.5rem;">
      <button type="submit">Add Project</button>
    </form>

    <p>'Current video games being watched (static for now)'</p>

    <table>
      <thead>
        <tr>
          <th>Game</th>
          <th>Source</th>
          <th>URL</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @projects.each do |project| %>
          <tr>
            <td><%= project[:name] %></td>
            <td><%= project[:source] %></td>
            <td><a href="<%= project[:url] %>" target="_blank"><%= project[:url] %></a></td>
            <td>
              <form action="/projects/<%= project[:name].parameterize %>" method="post" style="display: inline;">
                <input type="hidden" name="_method" value="DELETE">
                <button type="submit" onclick="return confirm('Delete?')">Delete</button>
              </form>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>